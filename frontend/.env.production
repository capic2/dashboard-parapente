# Production Environment Configuration
# This file is used during Docker build (npm run build)

# MSW Configuration
# Disable MSW mocking in production (use real API)
VITE_ENABLE_MSW=false

# API Configuration
# Leave empty to use dynamic URL detection from window.location
# The frontend will automatically detect the server at runtime
VITE_API_URL=
