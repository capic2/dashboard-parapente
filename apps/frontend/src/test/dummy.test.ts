/**
 * Dummy test file to prevent Vitest from failing when no tests are found
 * This project uses Storybook for component testing instead of unit tests
 * 
 * TODO: Add proper unit tests when needed
 */
import { describe, it, expect } from 'vitest';

describe('Dummy Test Suite', () => {
  it('should pass', () => {
    expect(true).toBe(true);
  });
});
