# Use Shared Fixtures in Backend Tests

Prefer reusable test fixtures and loader helpers for backend tests to reduce duplication and keep test data stable across test modules.

## Scope

Backend tests under `apps/backend/tests/**` and shared fixture definitions in `apps/backend/tests/fixtures/**`.


